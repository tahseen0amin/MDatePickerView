import XCTest
@testable import MDatePickerView

final class MDatePickerViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MDatePickerView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
